import 'package:flutter/material.dart';

class AppColors
{
static const Color  primaryColor   = Color(0xFF);
static const Color  secondaryColor   = Color(0xFF);
static const Color  thirdColor   = Color(0xFF);
}